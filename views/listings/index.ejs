<% layout("/layouts/boilerplate.ejs") %>
<h3 class="mb-3 mt-3">All Listings</h3>
<div class="row">
  <% for(let list of allListings) { %>
  <div class="card listing-card col-lg-4 col-md-6 col-12 mb-4 mx-auto">
    <a class="card-click" href="/listings/<%= list._id %>">
      <img
        src="<%= list.image.url %>"
        class="card-img-top"
        alt="listing_image"
        style="height: 20rem"
      />
      <div class="card-body">
        <p class="card-text">
          <b><%= list.title %></b><br />
          &#x20B9; <%= list.price.toLocaleString("en-IN") %> / night
        </p>
      </div>
    </a>
  </div>
  <% } %>
</div>
